# Direct dependencies for the bounded vectorbt feasibility experiment.
vectorbt==1.1.0
# vectorbt 1.1.0 imports a removed scattermapbox template under Plotly 7.0.0.
plotly==6.9.0
pytest==9.1.1

# Financial return and benchmark statistics.
empyrical-reloaded==0.5.12

# Agent orchestration.
openai-agents==0.22.0
# Already installed transitively; explicit for DSA tool-schema validation.
jsonschema==4.26.0

# Existing environment version, now explicitly used for limited Episode signatures.
cryptography==50.0.1

# Reproducible macOS Apple Silicon production runtime build.
pyinstaller==6.22.2

# Public research quotes only. MIT. Not a quote SLA and not Evidence input.
akshare==1.18.91
